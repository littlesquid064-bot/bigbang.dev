import React from 'react';import { mockReportData } from '../data/mockData';import ChartPlaceholder from '../components/ChartPlaceholder';import Accordion from '../components/Accordion';import Button from '../components/Button';import { Download, ExternalLink } from 'lucide-react';function ReportsPage() {const { overallScore, categoryScores, strengths, weaknesses, recommendations, resources } = mockReportData;const handleDownload = () => {alert('Downloading report PDF...');};return (<div className="container"><h1 className="page-title">Your AI Readiness Report</h1><div className="score-display"><h2>{overallScore}%</h2><p>Overall AI Readiness Score</p></div><div style={{ display: 'flex', justifyContent: 'center', marginBottom: '30px' }}><Button onClick={handleDownload}><Download size={18} style={{ marginRight: '8px' }} /> Download Report PDF</Button></div><div className="report-section"><ChartPlaceholder title="Category Scores Overview" type="radar" dataDescription="Your performance across key dimensions." /></div><div className="report-section"><h3>Strengths</h3><ul>{strengths.map((strength, index) => (<li key={index}>{strength}</li>))}
</ul></div><div className="report-section"><h3>Weaknesses</h3><ul>{weaknesses.map((weakness, index) => (<li key={index}>{weakness}</li>))}
</ul></div><div className="report-section"><h3>Tailored Recommendations</h3>{recommendations.map((rec, index) => (<Accordion key={index} title={rec.title}><p>{rec.description}</p></Accordion>))}
</div><div className="report-section"><h3>Curated Resources</h3><ul>{resources.map((resource, index) => (<li key={index}><a href={resource.url} target="_blank" rel="noopener noreferrer">{resource.name} <ExternalLink size={14} style={{ marginLeft: '5px', verticalAlign: 'middle' }} /></a></li>))}
</ul></div></div>);
}export default ReportsPage;