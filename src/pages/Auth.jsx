import React, { useState } from 'react';import { useNavigate } from 'react-router-dom';import Input from '../components/Input';import Button from '../components/Button';function Auth({ onLogin }) {const [isRegister, setIsRegister] = useState(false);const [email, setEmail] = useState('');const [password, setPassword] = useState('');const [confirmPassword, setConfirmPassword] = useState('');const navigate = useNavigate();const handleSubmit = (e) => {e.preventDefault();if (isRegister) {// Simulate registrationonLogin('Jane Doe');console.log('Registering:', { email, password });} else {// Simulate loginonLogin('Jane Doe');console.log('Logging in:', { email, password });}navigate('/dashboard');};return (<div className="auth-page"><div className="auth-card"><h2>{isRegister ? 'Register' : 'Login'}</h2><form onSubmit={handleSubmit}><Input label="Email" id="email" type="email" value={email} onChange={(e) => setEmail(e.target.value)} required /><Input label="Password" id="password" type="password" value={password} onChange={(e) => setPassword(e.target.value)} required />{isRegister && (<Input label="Confirm Password" id="confirmPassword" type="password" value={confirmPassword} onChange={(e) => setConfirmPassword(e.target.value)} required />)}<Button type="submit" style={{ width: '100%', marginTop: '20px' }}>{isRegister ? 'Register' : 'Login'}</Button></form><div className="form-footer"><p>{isRegister ? 'Already have an account?' : 'Don\'t have an account?'}{' '}<a href="#" onClick={() => setIsRegister(!isRegister)}>{isRegister ? 'Login' : 'Register'}</a></p><p><a href="#">Forgot Password?</a></p></div></div></div>);};export default Auth;