import React, { useState } from 'react';import Input from '../components/Input';import Button from '../components/Button';import Textarea from '../components/Textarea';import Alert from '../components/Alert';import { User, Save } from 'lucide-react';function ProfilePage() {const [name, setName] = useState('TestUser');const [email, setEmail] = useState('test@example.com');const [organization, setOrganization] = useState('AI Readiness Inc.');const [industry, setIndustry] = useState('Software & Tech');const [bio, setBio] = useState('Passionate about leveraging AI for digital transformation.');const [showAlert, setShowAlert] = useState(false);const [alertMessage, setAlertMessage] = useState('');const [alertType, setAlertType] = useState('info');const handleSave = (e) => {e.preventDefault();setShowAlert(false);setAlertMessage('');setAlertType('info');console.log('Profile saved:', { name, email, organization, industry, bio });setAlertMessage('Profile updated successfully!');setAlertType('success');setShowAlert(true);setTimeout(() => setShowAlert(false), 3000);};return (<div className="container"><h1 className="page-title"><User size={32} style={{ marginRight: '10px', verticalAlign: 'middle' }} /> Your Profile</h1>{showAlert && <Alert type={alertType} message={alertMessage} />}<form onSubmit={handleSave} className="profile-details"><Input label="Full Name" id="profile-name" value={name} onChange={(e) => setName(e.target.value)} /><Input label="Email Address" id="profile-email" type="email" value={email} onChange={(e) => setEmail(e.target.value)} disabled /><Input label="Organization" id="profile-org" value={organization} onChange={(e) => setOrganization(e.target.value)} /><Input label="Industry" id="profile-industry" value={industry} onChange={(e) => setIndustry(e.target.value)} /><Textarea label="Bio" id="profile-bio" value={bio} onChange={(e) => setBio(e.target.value)} /><Button type="submit"><Save size={18} style={{ marginRight: '8px' }} /> Save Changes</Button></form></div>);
}export default ProfilePage;