import React from 'react';import { Download } from 'lucide-react';import Accordion from '../components/Accordion';import Button from '../components/Button';import { mockReportData } from '../data/mockData';function Reports() {const { overallScore, categoryScores, generalRecommendations, resources } = mockReportData;const handleDownloadReport = () => {alert('Downloading PDF report...');};return (<div className="container"><h1 style={{ marginBottom: '30px' }}>AI Readiness Report</h1><div className="card score-display"><h2 style={{ color: 'white' }}>Your Overall AI Readiness Score</h2><div className="score-value" style={{ color: 'white' }}>{overallScore}</div><p className="score-description" style={{ color: 'rgba(255,255,255,0.9)' }}>Based on your assessment, here's a detailed breakdown of your AI readiness.</p><Button onClick={handleDownloadReport} className="btn-outline" style={{ marginTop: '20px', borderColor: 'white', color: 'white' }}><Download size={18} />Download Report PDF</Button></div><div className="card" style={{ marginTop: '20px' }}><div className="card-header">Detailed Category Analysis</div>{categoryScores.map((category) => (<Accordion key={category.name} title={`${category.name} - Score: ${category.score}%`}><h3>Strengths</h3><p>{category.strength}</p><h3>Weaknesses</h3><p>{category.weakness}</p><h3>Recommendations</h3><ul>{category.recommendations.map((rec, i) => (<li key={i}>{rec}</li>))}</ul></Accordion>))}</div><div className="card report-section"><div className="card-header">General Recommendations</div><ul>{generalRecommendations.map((rec, i) => (<li key={i}>{rec}</li>))}</ul></div><div className="card report-section"><div className="card-header">Curated Resources</div><ul>{resources.map((res, i) => (<li key={i}><a href={res.url} target="_blank" rel="noopener noreferrer">{res.title}</a></li>))}</ul></div></div>);};export default Reports;