import React from 'react';import { mockBenchmarkingData } from '../data/mockData';import ChartPlaceholder from '../components/ChartPlaceholder';function BenchmarkingPage() {const yourOrg = mockBenchmarkingData[0];const industryAverages = mockBenchmarkingData.slice(1);return (<div className="container"><h1 className="page-title">AI Readiness Benchmarking</h1><div className="report-section" style={{ marginBottom: '30px' }}><h3 style={{ textAlign: 'center' }}>Your Score vs. Industry Average</h3><ChartPlaceholder title="Overall Comparison" type="bar" dataDescription="Your organization's score compared to industry averages." /></div><div className="report-section"><h3 style={{ textAlign: 'center' }}>Detailed Peer Comparison</h3><div className="benchmark-table-container"><table className="benchmark-table"><thead><tr><th>Industry / Category</th>{yourOrg.categoryBreakdown.map((cat) => (<th key={cat.name}>{cat.name}</th>))}
<th>Overall Score</th></tr></thead><tbody>{mockBenchmarkingData.map((data) => (<tr key={data.id} className={data.industry === 'Your Organization' ? 'your-score' : ''}><td>{data.industry}</td>{data.categoryBreakdown.map((cat) => (<td key={cat.name}>{cat.score}%</td>))}
<td>{data.avgScore}%</td></tr>))}
</tbody></table></div></div></div>);
}export default BenchmarkingPage;