import React, { useState } from 'react';import { Link, useNavigate } from 'react-router-dom';import Input from '../components/Input';import Button from '../components/Button';import Alert from '../components/Alert';function LoginPage({ onLogin, isSignup = false }) {const [email, setEmail] = useState('');const [password, setPassword] = useState('');const [confirmPassword, setConfirmPassword] = useState('');const [error, setError] = useState('');const navigate = useNavigate();const handleSubmit = (e) => {e.preventDefault();setError('');if (isSignup) {if (password !== confirmPassword) {setError('Passwords do not match.');return;}// Simulate signup logicconsole.log('Signup attempted for:', email);onLogin(email.split('@')[0]); // Use part of email as username} else {// Simulate login logicif (email === 'test@example.com' && password === 'password') {onLogin('TestUser');} else {setError('Invalid email or password.');}}};return (<div className="form-container"><h2 className="page-title">{isSignup ? 'Register' : 'Login'} to AI Readiness Index</h2>{error && <Alert type="error" message={error} />}<form onSubmit={handleSubmit}><Input label="Email" id="email" type="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="your@email.com" required /><Input label="Password" id="password" type="password" value={password} onChange={(e) => setPassword(e.target.value)} placeholder="********" required />{isSignup && (<Input label="Confirm Password" id="confirmPassword" type="password" value={confirmPassword} onChange={(e) => setConfirmPassword(e.target.value)} placeholder="********" required />)}<Button type="submit">{isSignup ? 'Register' : 'Login'}</Button></form>{isSignup ? (<Link to="/login" className="form-link">Already have an account? Login</Link>) : (<><Link to="/signup" className="form-link">Don't have an account? Register</Link><Link to="/forgot-password" className="form-link">Forgot Password?</Link></>)}
</div>);
}export default LoginPage;