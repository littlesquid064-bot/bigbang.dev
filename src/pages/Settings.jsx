import React, { useState } from 'react';import Input from '../components/Input';import Button from '../components/Button';import Notification from '../components/Notification';function Settings() {const [notification, setNotification] = useState(null);const [password, setPassword] = useState('');const [newPassword, setNewPassword] = useState('');const [confirmNewPassword, setConfirmNewPassword] = useState('');const handlePasswordChange = (e) => {e.preventDefault();if (newPassword !== confirmNewPassword) {setNotification({ message: 'New passwords do not match.', type: 'error' });return;}console.log('Changing password to:', newPassword);setNotification({ message: 'Password updated successfully!', type: 'success' });setPassword('');setNewPassword('');setConfirmNewPassword('');};return (<div className="container"><h1 style={{ marginBottom: '30px' }}>Settings</h1>{notification && (<Notification message={notification.message} type={notification.type} onClose={() => setNotification(null)} />)}<div className="card"><div className="card-header">Change Password</div><form onSubmit={handlePasswordChange} style={{ maxWidth: '400px', margin: '0 auto' }}><Input label="Current Password" id="currentPassword" type="password" value={password} onChange={(e) => setPassword(e.target.value)} required /><Input label="New Password" id="newPassword" type="password" value={newPassword} onChange={(e) => setNewPassword(e.target.value)} required /><Input label="Confirm New Password" id="confirmNewPassword" type="password" value={confirmNewPassword} onChange={(e) => setConfirmNewPassword(e.target.value)} required /><Button type="submit" style={{ marginTop: '20px', width: '100%' }}>Update Password</Button></form></div><div className="card" style={{ marginTop: '20px' }}><div className="card-header">Notification Preferences</div><p>Manage your email notification settings here.</p><Button className="btn-secondary" style={{ marginTop: '10px' }}>Save Preferences</Button></div></div>);};export default Settings;