import React from 'react';import { Home, ListOrdered, Package, Settings } from 'lucide-react';function BottomNavBar({currentPage,navigateTo}) {const navItems = [{name: 'Dashboard',icon: Home,page: 'dashboard',},{name: 'Orders',icon: ListOrdered,page: 'orders',},{name: 'Inventory',icon: Package,page: 'inventory',},{name: 'Settings',icon: Settings,page: 'settings',},];return (<nav className="bottom-nav-bar"> {navItems.map((item) => (<buttonkey={item.page}className={`nav-item ${currentPage === item.page ? 'active' : ''}`}onClick={() => navigateTo(item.page)}><item.icon size={22} /> {item.name} </button>))} </nav>);}export default BottomNavBar;