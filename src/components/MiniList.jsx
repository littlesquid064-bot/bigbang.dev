import React from 'react';function MiniList({title,items,renderItem,onViewAll}) {return (<div className="mini-list"> <h3 className="mini-list-title">{title}</h3> {items.length > 0 ? (items.map((item, index) => (<div key={item.id || index} className="mini-list-item"> {renderItem(item)} </div>))) : (<div className="mini-list-item">No items to display.</div>)} {onViewAll && items.length > 0 && (<button className="button outline small" onClick={onViewAll} style={{marginTop: '15px'}}> View All </button>)} </div>);}export default MiniList;