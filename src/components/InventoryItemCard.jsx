import React from 'react';function InventoryItemCard({item,onClick}) {const isLowStock = item.currentStock <= item.reorderPoint;return (<div className="inventory-item-card" onClick={onClick}> <div className="inventory-item-card-header"> <span className="inventory-item-card-name">{item.name}</span> <span className={`inventory-item-card-stock ${isLowStock ? 'low' : 'ok'}`}> {item.currentStock} {item.unit} {isLowStock ? '(Low Stock)' : ''} </span> </div> <div className="inventory-item-card-details"> <p>Reorder Point: {item.reorderPoint} {item.unit}</p> </div> </div>);}export default InventoryItemCard;