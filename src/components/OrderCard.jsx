import React from 'react';import { formatDate } from '../data/utils';function OrderCard({order,onClick}) {const getStatusClass = (status) => {switch (status) {case 'new':return 'status-new';case 'preparing':return 'status-preparing';case 'ready':return 'status-ready';case 'delivered':return 'status-delivered';case 'cancelled':return 'status-cancelled';default:return '';}};const getPaymentStatusClass = (status) => {switch (status) {case 'pending':return 'payment-status-pending';case 'paid':return 'payment-status-paid';default:return '';}};return (<div className="order-card" onClick={onClick}> <div className="order-card-header"> <span className="order-card-customer-name">{order.customer.name}</span> <span className={`order-card-status ${getStatusClass(order.status)}`}> {order.status.charAt(0).toUpperCase() + order.status.slice(1)} </span> </div> <div className="order-card-details"> <p>Items: {order.items.length}</p> <p>Due: {formatDate(order.dueDate)}</p> <p>Payment: <span className={getPaymentStatusClass(order.paymentStatus)}>{order.paymentStatus.charAt(0).toUpperCase() + order.paymentStatus.slice(1)}</span></p> </div> <div className="order-card-price"> ${order.totalPrice.toFixed(2)} </div> </div>);}export default OrderCard;