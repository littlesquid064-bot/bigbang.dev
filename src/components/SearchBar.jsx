import React from 'react'; import { Search, X } from 'lucide-react'; function SearchBar({ searchTerm, onSearchChange, onClearSearch }) { return ( <div className="search-bar-container"> <Search size={20} /> <input type="text" placeholder="Search recipes by name, ingredient, or cuisine..." value={searchTerm} onChange={(e) => onSearchChange(e.target.value)} /> {searchTerm && ( <button onClick={onClearSearch}> <X size={20} /> </button> )} </div> ); } export default SearchBar;